<!DOCTYPE html>
<html>
<head>
  <title>Splitwise</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    
    <ul class="nav">
      <li class="block logo">Splitwise</li>
      <% if signed_in? %>
        <li class="block user-name">Welcome, <%= current_user.fname %> <%= current_user.lname %></li>
        <li class="block logout-pad"><%= link_to 'Logout', '/logout', { method: :delete, class: 'logout-btn'} %></li>
      <% else %>
       <%= link_to 'Login', '/login', {method: :get, class: 'logout-btn'} %>
     <% end %>
    <ul>
  </header>
  
  <div class="container">
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>"><%= value %></div>
    <% end %>

    <%= yield %>
  </div>
  <% if signed_in? %>
    
  <% end %>
</body>
</html>
